#include <wiringPiSPI.h>
#include <math.h>
#include <stdio.h>

#define SPI_SPEED 5000000

int main(){
  printf("Hello World\n");

  int status = wiringPiSPISetup(0, SPI_SPEED); 
  if(status == -1){
    printf("Got an error with setting up wiringPi SPI");
  }
  
  return 0;
  
}


